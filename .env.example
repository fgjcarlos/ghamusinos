# Entorno de ejecución: development | production
ENV=development

# Puerto TCP del servidor HTTP
PORT=8080

# Cadena de conexión a PostgreSQL (OBLIGATORIA)
DATABASE_URL=postgres://ghamusinos:ghamusinos@localhost:5432/ghamusinos?sslmode=disable
